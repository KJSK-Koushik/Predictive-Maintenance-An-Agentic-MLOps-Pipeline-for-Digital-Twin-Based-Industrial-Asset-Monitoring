extends: default

ignore: |
  .venv/
  Data/
  data/

rules:
  document-start: disable
  line-length:
    max: 100
    level: warning
  truthy:
    allowed-values: ["true", "false", "on"]
    check-keys: false
